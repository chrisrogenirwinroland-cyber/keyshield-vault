@"
services:
  api:
    image: YOUR_DOCKERHUB_USER/keyshield-api:${IMAGE_TAG}
    ports:
      - "3000:3000"

  frontend:
    image: YOUR_DOCKERHUB_USER/keyshield-frontend:${IMAGE_TAG}
    ports:
      - "4200:80"
"@ | Set-Content -Encoding utf8 ".\docker-compose.staging.yml"
